import React from 'react'

import { ModalProps } from '@/interfaces/modalType'

const DeleteTransactionModal: React.FC<ModalProps> = (props) => {
  return (
    <div>DeleteTransactionModal</div>
  )
}

export default DeleteTransactionModal